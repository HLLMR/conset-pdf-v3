# Open Questions and Risks

Rolling backlog of unresolved questions and migration risks.

## Priority Key

- P0: Blocks progress or architecture
- P1: High risk, should be resolved in current phase
- P2: Can defer with mitigation

## Items

Only genuine unresolved product/code risks should remain in this table. Documentation coverage gaps identified by Phase 8 have been closed via addenda to Phases 4 and 7.

| ID | Priority | Phase Raised | Topic | Question or Risk | Proposed Owner | Resolution Target | Status |
|---|---|---|---|---|---|---|---|
| R-001 | P1 | Bootstrap | Scope | Confirm final V4 GUI runtime decision and cutover timeline in master plan. | Architecture | Phase 3 | OPEN |
| R-002 | P1 | Phase 2 | Specs Footer Anchoring | Footer-first segmentation exists in design, but `buildFooterSectionMap()` is currently stubbed; deterministic section boundary behavior may diverge from intended architecture until fully implemented. | Core Specs | Phase 3 | OPEN |
| R-003 | P1 | Phase 2 | Footer Parser Coverage | `parseFooterSectionId()` currently recognizes Division 23 patterns only; this can under-detect section boundaries for other divisions in mixed spec books. | Core Specs | Phase 3 | OPEN |
| R-004 | P2 | Phase 2 | Quality-Driven Fallback | Transcript extractor fallback is availability/error based, not quality-gate based; low-quality successful extraction is not automatically retried with alternate backend. | Transcript | Phase 3 | RETIRED (Phase 8): V4 uses a single PDFium extraction path, so quality-driven fallback is no longer a required architecture branch. |
| R-005 | P2 | Phase 2 | Schedule Extraction Completeness | Schedule module is geometry-first with TODO fallbacks (pdfplumber/camelot) and no explicit merged-cell or rotated-header modeling, risking partial extraction on complex schedules. | Transcript/Schedules | Phase 5 | OPEN |
| R-006 | P1 | Phase 3 | Merge Sidecar Maturity | Planning docs describe disk-based streaming merge via pikepdf sidecar, but current executable path still assembles merged output in memory with `pdf-lib` and only uses pikepdf for final passthrough writing. This affects ADR-001 accuracy now and ADR-004 scoping later. | Core Merge | Phase 3 | ADDRESSED (Phase 7): V4 mandates disk-streaming merge via lopdf from day one; in-memory assembly is explicitly removed. See phase-07-sidecar-replacement-plan.md. |
| R-007 | P2 | Phase 3 | Workflow State Reuse | Architecture intent emphasizes analyze-then-correct-then-execute without reparsing, but current workflow implementations re-run `analyze()` inside `applyCorrections()` and merge execute only weakly reuses analyzed plan state. This adds latency and weakens the separation between reviewed state and execution cursor. | Workflow Engine | Phase 6 | ADDRESSED (Phase 7): V4 Workflow trait contract explicitly requires apply_corrections() to mutate prior InventoryResult without re-running analyze(). See phase-07-rust-port-primer.md. |
| R-008 | P2 | Phase 3 | Extractor-Path Determinism Gap | Determinism tests enforce strict hash equality for PyMuPDF but explicitly tolerate coordinate/hash variance for PDF.js fallback. This creates a conditional determinism guarantee in v3 that should be eliminated in Rust or clearly surfaced in runtime quality gates. | Transcript | Phase 7 | ADDRESSED (Phase 7): V4 has a single extraction path (PDFium only; no fallback). The path-conditional determinism guarantee is eliminated. See phase-07-rust-port-primer.md. |
| R-009 | P2 | Phase 3 | Auto-Layout Capability Drift | CLI/docs expose `--auto-layout` and profile suggestion language, but merge command execution currently only consumes explicit layout file or inline ROI options and does not execute auto-profile generation/selection. This can mislead users and distort migration readiness assumptions. | CLI + Workflow | Phase 6 | RESOLVED (Phase 9): Confirmed dead flag. `options.autoLayout` never referenced in handler. Classified as no-op in phase-09-doc-drift-matrix.md §3 and phase-09-deprecation-gates.md §4. Rust must not expose this flag until implemented. |
| R-010 | P1 | Phase 3 | Privacy Mode Policy Drift | Privacy docs can be read as unconditional "no sensitive data to LLM," but runtime supports `FULL_TEXT_OPT_IN` and caller context injection. Without explicit policy gating and UI/CLI safeguards, deployments can unintentionally widen data exposure. | Transcript + ML | Phase 7 | ADDRESSED (Phase 7): FULL_TEXT_OPT_IN mandated to require explicit CLI flag and user confirmation. Token vault mode must appear in AuditBundle. See phase-07-rust-port-primer.md. |
| R-011 | P2 | Phase 3 | Technology Lifecycle Labeling Drift | "Abandoned" and "replaced" labels in planning/docs are not uniformly aligned with executable code. Some paths are superseded but still active (for example pdf-lib bookmark generation in merge apply, PDF.js fallback/bookmark surfaces, command registration mismatches). This can cause migration teams to miss live code paths that still need parity or decommissioning. | Architecture + Docs | Phase 6 | RESOLVED (Phase 9): `phase-09-doc-drift-matrix.md` and its migration-gate truth table distinguish active, partial, dead-code, and deferred states explicitly. |
| R-012 | P2 | Phase 4 | IPC Error Envelope Doc Drift | GUI IPC docs still describe `IpcResponse.error` as a plain string while executable code uses structured error payload `{ message, code?, stack?, context? }` and preload preserves those fields in thrown `BridgeError`. Migration planning or client integrations that rely on docs-only shape can silently lose error metadata handling. | GUI IPC + Docs | Phase 9 | RESOLVED (Phase 9): Correct structured contract documented in phase-09-ux-critical-path.md §2. IpcResponse<T> shape with error.{message, code?, stack?, context?} is canonical. wrapIpcHandler utility usage mandated for all handlers. |
| R-013 | P1 | Phase 5 | Roadmap Status Drift | `ROADMAP.md` and `conset-pdf-gui/ROADMAP.md` still contain stale status claims (for example split/bookmark/settings placeholders, specs-patch abandoned labeling) that conflict with executable code paths and can mis-scope migration parity. | Architecture + Docs | Phase 9 | RESOLVED (Phase 9): Full divergence matrix documented in phase-09-doc-drift-matrix.md. Migration-gate truth table in §8 is the authoritative implementation status for Rust planning. 6 doc-fix items captured as R-019. |
| R-014 | P1 | Phase 5 | Submittal Orchestration Gap | Submittal parsing APIs exist, but there is no workflow engine (`analyze/applyCorrections/execute`), CLI command, or GUI route. Rust planning needs an explicit decision on whether submittals are promoted to first-class workflow in V4 scope. | Product + Core Workflows | Phase 7 | ADDRESSED (Phase 7): Recommended for V4 Phase 4 first-class workflow. Minimum scope: SubmittalWorkflow struct + SubmittalChromeMetadata struct stubs in contracts crate. See phase-07-rust-port-primer.md. |
| R-015 | P1 | Phase 8 | Module Map Gaps (10 files) | `text/bandSlicer.ts`, `text/pageRegions.ts`, `specs/extract/tableDetector.ts`, `specs/footerIndexBuilder.ts`, `specs/inventory/specFooterIndexer.ts`, `specs/inventory/specSectionizer.ts`, `bookmarks/settings.ts`, `bookmarks/profiles/raw.ts`, `ipc/logging.ts`, `ipc/merge-internal.ts` were absent from the initial Phase 7 module map. | V4 Phase 0 | Phase 7 addendum | ADDRESSED: Phase 7 module map updated to include all missing files and the corrected `specs/inventory/index.ts` path. |
| R-016 | P1 | Phase 8 | Phase 5 GUI Deferral Gaps | Report Viewer GUI, Automated ROI detection UI, and Schedule Extraction UI had no explicit deferral notes in the initial Phase 7 handoff. | Product + GUI | Phase 7 addendum | ADDRESSED: Phase 7 primer now carries explicit deferral notes and requires `ScheduleTable` in `crates/contracts` from Phase 1. |
| R-017 | P2 | Phase 8 | AuditBundle Schema Undefined | `AuditBundle` was referenced in Phase 7 as a contracts crate type but lacked a full JSON schema and field list. | Core Contracts | Phase 4 addendum | ADDRESSED: `04-contracts/phase-04-audit-bundle-schema.md` is now the authoritative wire contract. |
| R-018 | P2 | Phase 8 | Logging Constraints Not Captured | `conset-pdf-gui/docs/LOGGING_IMPLEMENTATION.md` contains concrete V4-relevant logging constraints (5×10MB rotation, export bundle format, panic hook, session markers) not reproduced anywhere in the extraction library. These are implementation constraints for V4 Rust tracing configuration. | Ops/Supportability | Phase 9 ops-telemetry | RESOLVED (Phase 9): All constraints captured in phase-09-ops-telemetry-lessons.md §1 and §4. Clarified: rotation is 5×10MB (not 2MB — LOGGING_IMPLEMENTATION.md is authoritative over any other mention). Rust tracing crate baseline defined in §6. |

## Postmortem-External Follow-Ups
| ID | Priority | Phase Raised | Topic | Question or Risk | Proposed Owner | Resolution Target | Status |
|---|---|---|---|---|---|---|---|
| R-019 | P2 | Phase 9 | Doc-Fixing Backlog | 6 documentation errors identified in Phase 9 drift analysis: (1) ROADMAP table incorrectly marks Extract Documents and Fix Bookmarks GUI as ✅; (2) README stale "Partially Implemented" for split; (3) ROADMAP duplicate Specs Patch entry; (4) CLI.md does not note --auto-layout and --save-layout as unimplemented; (5) debugWalkthrough.ts and specsInventory.ts commands undocumented; (6) ROADMAP date inconsistency. These do not block Rust planning but should be fixed before any external review. | Docs | Post-Phase 9 | OPEN |

These are outside the postmortem artifact set. They are repo-doc cleanup tasks, not gaps in the extraction library.
