# Source of Truth Decisions

Record conflicts between docs and code, then mark the canonical source used by post-mortem extraction.

## Decision Log

| ID | Topic | Conflicting Sources | Canonical Source Chosen | Rationale | Date |
|---|---|---|---|---|---|
| SOT-001 | Workflow status drift | `docs/CLI.md`, `README.md`, `ROADMAP.md` | Code + tests first, then `ROADMAP.md` | Some docs have stale command status claims. | 2026-03-19 |
| SOT-002 | Transcript fallback chain details | `docs/TRANSCRIPT_ARCHITECTURE.md` (mentions PDFium in flow), `packages/core/src/transcript/*` | Code in `transcript/factory.ts` and active extractors | Current executable fallback chain is PyMuPDF -> PDF.js; PDFium extractor is not present in active implementation. | 2026-03-19 |
| SOT-003 | Footer-first section anchoring completeness | `docs/TRANSCRIPT_ARCHITECTURE.md`, `packages/core/src/specs/footerSectionMap.ts` | Code in `specs/footerSectionMap.ts` and `specs/extract/sectionDetector.ts` | Footer-first mode exists architecturally, but footer section map builder is currently a stub and not a full production boundary source. | 2026-03-19 |
| SOT-004 | Large-file merge sidecar status | `docs/largeFileRefactorPlan.md`, `docs/postMortemDocExtraction.md`, `packages/core/src/core/applyPlan.ts`, `packages/core/src/utils/pikepdfWriter.ts` | Code in `applyPlan.ts` and `utils/pikepdfWriter.ts` | Current merge flow uses in-memory `pdf-lib` page assembly, then routes final output through pikepdf passthrough. Fully disk-streaming sidecar merge is planned but not the active implementation. | 2026-03-19 |
| SOT-005 | Workflow state reuse maturity | `docs/WORKFLOWS.md`, `docs/postMortemDocExtraction.md`, `packages/core/src/workflows/*` | Code in `workflows/merge/mergeWorkflow.ts`, `workflows/specs-patch/specsPatchWorkflow.ts`, and `workflows/bookmarks/bookmarksWorkflow.ts` | Workflow staging is implemented, but concrete `applyCorrections()` paths re-run `analyze()` and merge execute does not primarily execute from previously analyzed plan state. The pattern exists, but plan/cursor reuse is only partially realized. | 2026-03-19 |
| SOT-006 | Determinism guarantee scope in prototype | `docs/MASTER_PLAN_v4.md`, `docs/postMortemDocExtraction.md`, `tests/transcript/determinism.test.ts`, `packages/core/src/transcript/*` | Code + tests in transcript canonicalization and determinism suite | Determinism controls are strong on the PyMuPDF path, but tests explicitly tolerate reduced strictness for PDF.js fallback due to coordinate precision variance. Prototype determinism is therefore path-conditional, not uniformly strict across extractors. | 2026-03-19 |
| SOT-007 | Auto-layout/profile-selection implementation scope | `docs/CLI.md`, `docs/postMortemDocExtraction.md`, `packages/cli/src/commands/mergeAddenda.ts`, `packages/core/src/workflows/merge/mergeWorkflow.ts`, `packages/core/src/transcript/profiles/registry.ts` | Executable CLI/workflow code in merge command and merge workflow | CLI/docs language implies auto-layout/profile suggestion capability, but merge execution currently only uses explicit layout or inline ROI. Transcript profile registry matching exists but is not wired into merge locator selection. | 2026-03-19 |
| SOT-008 | Privacy guarantee scope for ML compiler path | `docs/ML_RULESET_COMPILER.md`, `docs/TRANSCRIPT_ARCHITECTURE.md`, `packages/core/src/transcript/abstraction/*`, `packages/core/src/transcript/ml/*` | Code in sanitize/tokenVault/compiler pipeline plus tests | Strict placeholder-first privacy behavior is implemented and default, but guarantee is mode-dependent: `FULL_TEXT_OPT_IN` and caller-supplied context can intentionally include literal text. Documentation should state conditional scope rather than absolute claim. | 2026-03-19 |
| SOT-009 | Abandoned/superseded status precision | `docs/postMortemDocExtraction.md`, `docs/CLI.md`, `packages/core/src/core/applyPlan.ts`, `packages/core/src/transcript/factory.ts`, `packages/core/src/analyze/documentContext.ts`, `packages/core/src/workflows/specs-patch/specsPatchWorkflow.ts`, `packages/cli/src/cli.ts` | Executable code across merge/extraction/specs-patch/CLI registration | Multiple "abandoned" or "replaced" labels are directionally correct but over-absolute. In code, several paths are superseded-yet-active (not fully removed), and some commands labeled abandoned in docs are still registered and executable. Lifecycle labels must distinguish deprecated, partial, and removed states. | 2026-03-19 |
| SOT-010 | IPC response error shape | `conset-pdf-gui/docs/IPC_CONTRACTS.md`, `conset-pdf-gui/src/shared/ipc-response.ts`, `conset-pdf-gui/src/preload.ts` | Executable envelope code in `src/shared/ipc-response.ts` and `src/preload.ts` | Docs describe `error?: string`, but code implements structured error objects (`message`, optional `code`, `stack`, `context`) and preload preserves those fields on thrown `BridgeError`. Postmortem contracts and migration work should follow executable shape. | 2026-03-19 |
| SOT-011 | GUI workflow completion status drift | `conset-pdf-gui/ROADMAP.md`, `conset-pdf-gui/src/modules/app/viewManager.js`, `conset-pdf-gui/src/split-drawings-wizard.js`, `conset-pdf-gui/src/bookmark-wizard.js`, `conset-pdf-gui/src/settings-view.js` | Executable GUI routing and wizard/view implementations | ROADMAP still labels several areas as placeholder/TODO that are already implemented in code (split wizard, bookmark wizard, settings view). Migration planning should use executable routing and handler wiring as truth. | 2026-03-19 |
| SOT-012 | Submittal feature maturity boundary | `docs/postMortemDocExtraction.md`, `conset-pdf/packages/core/src/submittals/extract/submittalParser.ts`, `conset-pdf/packages/cli/src/commands/`, `conset-pdf/packages/core/src/workflows/` | Code-level boundary: parser APIs are real, workflow orchestration is absent | Planning text can be read as broad submittal workflow intent, but current implementation is parser-level and marked experimental; there is no first-class workflow runner, CLI command, or GUI path. Treat as partial capability, not complete feature lane. | 2026-03-19 |

## Rules

1. Prefer executable truth in code and tests over prose docs.
2. If code and tests disagree, record as P0 risk in `open-questions-and-risks.md`.
3. Every major conflict must have a log entry before phase close.
