# Standards Refactor Cleanup Status

**Generated**: March 1, 2026  
**Refactor Scope**: Phases 3, 4, and 5 only (Field Naming + Legacy Support + Documentation)

## Summary

The refactor completed **Phases 3, 4, and 5** of the Standards Refactor Proposal. This was a **targeted enhancement**, not a "massive refactor". No orphan files were created that require deletion.

---

## Files Created (All Permanent)

### Test Files ✅ KEEP
- `tests/standards/field-naming-validation.test.ts` - Validates context-specific field naming conventions (9 tests)
- `tests/workflows/split-legacy.test.ts` - Integration tests for legacy  specs support (2 tests)

**Status**: These provide permanent test coverage for Phase 3 and Phase 4 work.

### Documentation ✅ KEEP
- `docs/FIELD_NAMING_GUIDE.md` - Comprehensive guide to context-specific field naming conventions
- `docs/LEGACY_SUPPORT_ARCHITECTURE.md` - Architecture documentation for pre-2004 MasterFormat support

**Status**: These are permanent reference docs for developers.

---

## Pre-Existing Files (Not Created By This Refactor)

### Generated Datasets ✅ CURRENTLY USED
The following `.generated.ts` files exist and **are actively used** by `standards/registry.ts`:

- `packages/core/src/standards/datasets/disciplines.generated.ts` (1447 lines)
  - Generated: 2026-03-01T18:24:42.603Z
  - Imported by: `registry.ts` (line 16-18)
  - Used by: `standardsRegistry` to populate discipline maps

- `packages/core/src/standards/datasets/divisions.generated.ts` (325 lines)
  - Generated: 2026-03-01T18:24:42.604Z
  - Imported by: `registry.ts` (line 20-22)
  - Used by: `standardsRegistry` to populate division maps

- `packages/core/src/standards/datasets/legacySections.generated.ts`
  - Imported by: `registry.ts` (line 24-27), `standards/index.ts` (line 17)
  - Used by: Phase 4 legacy support implementation

**Status**: These are working code, **DO NOT DELETE**. They are generated by the script but are part of the active codebase.

### Generation Script ✅ KEEP
- `scripts/generate-standards-datasets.ts` (305 lines)
  - Purpose: Generates the `.generated.ts` files from `/.reference/UDS.xlsx`
  - Last run: Unknown (but files have timestamp 2026-03-01T18:24:42)
  - Command: `npx tsx scripts/generate-standards-datasets.ts`

**Status**: This is the source generator for the datasets. Keep for future regeneration.

---

## Unused/Orphan Code Analysis

### Registry Methods (Defined But Not Called)

The following methods in `standards/registry.ts` are **defined but not currently called** anywhere:

1. **`resetToDefaults()`** (line ~255)
   - Purpose: Clear all user customizations and reload defaults
   - Intended for: Phase 2 GUI reset functionality
   - Status: ⚠️ Future-proofing for Phase 1/2 implementation

2. **`addDisciplineAlias(alias)`** (line ~318)
   - Purpose: Add a discipline alias dynamically
   - Intended for: Phase 2 GUI alias management
   - Status: ⚠️ Future-proofing for Phase 1/2 implementation

3. **`removeDisciplineAlias(alias)`** (line ~325)
   - Purpose: Remove a discipline alias dynamically
   - Intended for: Phase 2 GUI alias management
   - Status: ⚠️ Future-proofing for Phase 1/2 implementation

4. **`getDivisionByCode(code)`** (line 337)
   - Purpose: Get division by 4-character CODE (PLUM, HVAC, ELEC)
   - Intended for: Future lookup operations
   - Status: ⚠️ Implemented but no current use case

5. **`validateDisciplineCode(code)`** (line 466)
   - Purpose: Validate discipline code and return entry or reason
   - Intended for: Phase 2 GUI validation feedback
   - Status: ⚠️ Future-proofing for Phase 1/2 implementation

6. **`validateDivisionCode(code)`** (line 481)
   - Purpose: Validate division code and return entry or reason
   - Intended for: Phase 2 GUI validation feedback
   - Status: ⚠️ Future-proofing for Phase 1/2 implementation

**Recommendation**: ✅ **KEEP ALL** - These are not orphans. They are part of the complete StandardsRegistry API design. They will be used if/when Phase 1 and Phase 2 are implemented.

**Rationale**: The registry was designed with a complete API surface for the full refactor vision. Phases 3-5 use a subset of the API. Removing unused methods now would require re-adding them later if Phase 1/2 are ever implemented.

---

## Deleted Nothing? Correct!

**Q: Why are there no files marked for deletion?**

**A**: The refactor work (Phases 3, 4, 5) was **additive and modificative**, not replacement-based:

- **Phase 3**: Modified existing field naming in `bookmarks/treeBuilder.ts` and `core/splitSet.ts` (no deletions needed)
- **Phase 4**: Added legacy support features to `registry.ts`, `settings.ts`, splitSet, UI (no deletions needed)
- **Phase 5**: Added new tests and documentation (no deletions needed)

No old code was replaced or deprecated that would require cleanup.

---

## If Phase 1 & 2 Are NOT Going Forward...

If the decision is made to **NOT** implement Phase 1 (Core Standards Layer) and Phase 2 (GUI Integration):

### Code to Consider Removing ⚠️ (Only if Phase 1/2 cancelled)

1. **Unused Registry Methods**:
   - `resetToDefaults()`
   - `addDisciplineAlias()`
   - `removeDisciplineAlias()`
   - `getDivisionByCode()`
   - `validateDisciplineCode()`
   - `validateDivisionCode()`

2. **Unused Type Definitions in `types.ts`**:
   - `UserCustomizations` interface (if not used)
   - Several fields in `DisciplineEntry` like `userAlias`, `userOverride`
   - Several fields in `DivisionEntry` like `userTitle`, `userAlias`, `userOverride`

3. **Script `generate-standards-datasets.ts`**:
   - If the `.generated.ts` files are never regenerated, the script is not needed
   - However: Keeping it as reference/documentation is harmless

**Current Recommendation**: ⏸️ **DO NOT DELETE ANYTHING YET**

The "unused" methods and types are:
1. Not causing any problems
2. Part of a cohesive API design
3. May be useful in future
4. Not large enough to justify cleanup churn

---

## Action Items

### ✅ Immediate (Done)
- [x] Phase 3 field naming refactor complete
- [x] Phase 4 legacy support complete
- [x] Phase 5 validation tests complete
- [x] Phase 5 documentation complete
- [x] All builds passing
- [x] All tests passing (311/311)

### 🔍 Testing Phase (Current)
- [ ] Run full regression test suite
- [ ] Validate GUI settings UI with legacy sections table
- [ ] Test legacy PDF detection end-to-end
- [ ] Verify field naming changes don't break existing projects

### 📦 Future (If Phase 1/2 Implemented)
- [ ] The "unused" methods will be used
- [ ] No cleanup needed

### 🗑️ Future (If Phase 1/2 Cancelled)
- [ ] Review and remove unused registry methods
- [ ] Simplify type definitions to remove user-customization fields
- [ ] Document architectural decision to keep standards as-is

---

## Conclusion

**Cleanup Status**: ✅ **NO ORPHAN FILES TO DELETE**

All files created during this refactor serve a purpose:
- Tests provide coverage
- Documentation provides reference
- Pre-existing generated datasets are used by working code
- "Unused" methods are future-proofing, not orphans

**Recommendation**: Proceed with testing. No cleanup required at this time.
